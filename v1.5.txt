更新了回放逻辑：由原本的存储种子和移动记录录像改为由128个字符来存储32种出数和4中移动，所以即便修改了游戏随机算法也能够实现100%复现当前存储的回放了。不会暴露种子。

v1.5 更新日志：
1. 新增“练习板”（Practice Board）：
   - 支持自由放置任意方块（包括直接替换已有方块）。
   - 添加了新手引导蒙版。
   - 文件名已更改为 Practice_board.html。
2. 计时器与统计优化：
   - 新增 16 的计时统计。
   - 优化显示逻辑：当触发 32k 相关子计时器时，自动隐藏 16/32 计时器，保持界面整洁（限制12条展示）。
3. 已知问题修复与调整：
   - 修复了“撤销”操作可能导致的统计延迟。
   - 修复了“游戏结束”页面点击撤销会一次回退两步的 Bug。
   - 移除了主页的 [DEV] 测试按钮。
   - 优化了练习板的移动端适配和 UI 细节。
